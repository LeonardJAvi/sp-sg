%tr{ id: "row-#{project.id}" }
	-# - if can? :destroy_multiple, Project
	-# 	%td.item-check{style:"width: 10%"}
	-# 		= link_to "", admin_project_path(project), remote: true, class: "show-row"
	-# 		%input{ type:"checkbox", class: "filled-in", id: "checkbox-#{project.id}", 'checklist-model'=>"check", 'checklist-value'=>"#{project.id}", "ng-click"=>"selectByCheck(#{@objects.size})"}
	-# 		%label{ for: "checkbox-#{project.id}" }
	%td{style:"width: 2%"}
	%td{style:"width: 20%"}
		= link_to "", admin_project_path(project, search: @query), remote: true, class: "show-row"
		%span
			= project.name
	%td{style:"width: 10%"}
		= link_to "", admin_project_path(project, search: @query), remote: true, class: "show-row"
		%span
			= project.group
	-# - if can? :clone, Project
	-# 	%td{style:"width: 5%"}
			-# = link_to admin_project_clone_path(project), class: "btn-floating waves-effect btn-flat" do
			-# 	= material_icon.md_24.search.css_class("md-dark")
	%td{style:"width: 6%"}
		= link_to admin_project_path(project), class: "btn-floating waves-effect btn-flat" do
			= material_icon.md_24.visibility.css_class("md-dark")
